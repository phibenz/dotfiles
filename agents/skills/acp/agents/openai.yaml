interface:
  display_name: "Add, Commit, Push"
  short_description: "Delegate stage, commit, and push workflow"
  default_prompt: "Use $acp to delegate staging, committing, and pushing the intended changes."
