interface:
  display_name: "Add, Commit, Push"
  short_description: "Commit and push intended changes quickly"
  default_prompt: "Use $acp to stage, commit, and push the intended changes with the fast inline workflow."
