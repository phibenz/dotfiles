interface:
  display_name: "Worktree"
  short_description: "Create a worktree from the selected base and start Codex"
  default_prompt: "Use $wt to create a sibling worktree and start Codex in its narrower right Herdr pane."
