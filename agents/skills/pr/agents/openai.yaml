interface:
  display_name: "Pull Request"
  short_description: "Create or update pull requests quickly"
  default_prompt: "Use $pr to create or update the pull request for the current branch with the fast inline workflow."
